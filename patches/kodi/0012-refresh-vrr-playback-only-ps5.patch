ps5: refresh-rate selection = VRR during playback (with On start/stop), the
lowest multiple of the frame rate within 48-120 Hz; otherwise the system rate.
Replaces 0010's fixed-rate rule.

--- a/xbmc/windowing/Resolution.cpp	2026-09-26 16:22:53.640703605 +0000
+++ b/xbmc/windowing/Resolution.cpp	2026-09-26 16:23:25.997334337 +0000
@@ -141,41 +141,56 @@
     CLog::Log(LOGDEBUG, "[WHITELIST] No match for an exact resolution with an exact refresh rate");
 
 #if defined(TARGET_PS5)
-  // PS5: every output mode has the desktop (GUI) size; the refresh rates are
-  // the system rate (59.94 Hz) and, if available, 119.88 Hz. Pick the lowest
-  // integer multiple of the frame rate that a mode offers - the exact rate
-  // first, then 2x, 3x ... - so a higher rate (120 Hz) is used only when no
-  // lower one gives an even cadence (23.976/24 fps). Done here, before Kodi's
-  // own passes, so 1080p and 2160p videos choose alike (the 3:2 pulldown pass
-  // would otherwise take 59.94 Hz for a 2160p 24p film). Within 0.15%: 24.000
-  // fps takes 119.88 Hz and 60.000 fps 59.94 Hz ("Sync playback to display",
-  // when on, absorbs the 0.1%; the output rate never depends on it).
+  // PS5: the output runs at the system rate (the desktop mode). Only with
+  // "Adjust display refresh rate: On start/stop" does a video get a VRR mode
+  // (registered by the window system as "(PS5 VRR)"): the lowest multiple of
+  // its frame rate that reaches the PS5's VRR minimum of 48 Hz (23.976 ->
+  // 71.928, 24 -> 48, 25 -> 50, 29.97 -> 59.94), up to 120 Hz. Anything else
+  // keeps the system rate; fixed-rate modes other than the desktop are never
+  // chosen. "Sync playback to display" plays no part in this.
   {
     const RESOLUTION_INFO desktop =
         CServiceBroker::GetWinSystem()->GetGfxContext().GetResInfo(RES_DESKTOP);
-    CLog::Log(LOGDEBUG,
-              "[WHITELIST] Searching for a desktop-size mode at an integer multiple of {} fps", fps);
-    for (int multiple = 1; multiple <= 5; ++multiple)
+    const bool startStop = CServiceBroker::GetSettingsComponent()->GetSettings()->GetInt(
+                               CSettings::SETTING_VIDEOPLAYER_ADJUSTREFRESHRATE) ==
+                           ADJUST_REFRESHRATE_ON_STARTSTOP;
+    if (startStop && fps > 0.0f)
     {
-      for (const auto& mode : indexList)
+      int multiple = 1;
+      while (fps * multiple < 48.0f)
+        ++multiple;
+      const float target = fps * multiple;
+      if (target <= 120.5f)
       {
-        auto i = CDisplaySettings::GetInstance().GetResFromString(mode.asString());
-        const RESOLUTION_INFO info = CServiceBroker::GetWinSystem()->GetGfxContext().GetResInfo(i);
-        if (info.iScreenWidth == desktop.iScreenWidth &&
-            info.iScreenHeight == desktop.iScreenHeight &&
-            (info.dwFlags & D3DPRESENTFLAG_MODEMASK) == (curr.dwFlags & D3DPRESENTFLAG_MODEMASK) &&
-            MathUtils::FloatEquals(info.fRefreshRate, fps * multiple, 0.0015f * info.fRefreshRate))
+        // the closest "(PS5 VRR)" mode within 0.15% (30 fps -> 60, not 59.94)
+        int best = -1;
+        float bestDiff = 0.0f;
+        for (int i = RES_CUSTOM; i < static_cast<int>(CDisplaySettings::GetInstance().ResolutionInfoSize()); ++i)
         {
-          CLog::Log(LOGINFO, "[WHITELIST] Matched {}x the frame rate: {} ({})", multiple,
-                    info.strMode, i);
-          resolution = i;
+          const RESOLUTION_INFO info =
+              CServiceBroker::GetWinSystem()->GetGfxContext().GetResInfo(static_cast<RESOLUTION>(i));
+          const float diff = std::abs(info.fRefreshRate - target);
+          if (info.strMode.find("(PS5 VRR)") != std::string::npos &&
+              info.iScreenWidth == desktop.iScreenWidth &&
+              info.iScreenHeight == desktop.iScreenHeight && diff <= 0.0015f * info.fRefreshRate &&
+              (best < 0 || diff < bestDiff))
+          {
+            best = i;
+            bestDiff = diff;
+          }
+        }
+        if (best >= 0)
+        {
+          CLog::Log(LOGINFO, "[PS5] {} fps: VRR at {}x = {} ({})", fps, multiple,
+                    CServiceBroker::GetWinSystem()->GetGfxContext().GetResInfo(static_cast<RESOLUTION>(best)).strMode,
+                    best);
+          resolution = static_cast<RESOLUTION>(best);
           return;
         }
+        CLog::Log(LOGINFO, "[PS5] {} fps: no VRR mode at {:.3f} Hz: system rate {}", fps, target,
+                  desktop.strMode);
       }
     }
-    // no even cadence available (e.g. 25 fps): stay at the system rate
-    CLog::Log(LOGINFO, "[WHITELIST] No mode at an integer multiple of {} fps: keeping the "
-              "system rate {}", fps, desktop.strMode);
     resolution = RES_DESKTOP;
     return;
   }
